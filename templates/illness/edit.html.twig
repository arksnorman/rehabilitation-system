{% extends 'base.html.twig' %}

{% block title %}Edit Illness{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12 text-center">
            <h1 class="page-header">Edit Illness</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            {{ include('illness/_form.html.twig', {'button_label': 'Update'}) }}
            <br>
            <a href="{{ path('illness_index') }}" class="btn btn-primary">Back to illnesses list</a>
            <a href="{{ path('illness_delete', {'id': illness.id}) }}" class="btn btn-danger"
               onclick="return confirm('Are you sure you want to delete this item?');">
                <i class="fa fa-trash-o"></i> Delete
            </a>
        </div>
    </div>
{% endblock %}