{% extends 'base.html.twig' %}

{% block title %}Edit Therapy{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12 text-center">
            <h1 class="page-header">Edit Therapy</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            {{ include('therapy/_form.html.twig', {'button_label': 'Update'}) }}
            <br>
            <a href="{{ path('therapies') }}" class="btn btn-primary">Back to therapies list</a>
            <a href="{{ path('deleteTherapy', {'id': therapy.id}) }}" class="btn btn-danger"
               onclick="return confirm('Are you sure you want to delete this item?');">
                <i class="fa fa-trash-o"></i> Delete
            </a>
        </div>
    </div>
{% endblock %}